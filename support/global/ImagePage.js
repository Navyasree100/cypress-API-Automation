require('cypress-xpath')
class ImagePage
{
    imagePage_visit_RM(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/italy-lazio-rome-dolce-et-gabanna-shop-image209963.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/italy-lazio-rome-dolce-et-gabanna-shop-image209963.html')
    }

    imagePage_visit_vector(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-backpack-school-bag-and-book-design-121592052.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-backpack-school-bag-and-book-design-121592052.html')
    }

    imagePage_visit_RF(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/temples-of-different-architecture-patan-kathmandu-nepal-test6-image4998862.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/temples-of-different-architecture-patan-kathmandu-nepal-test6-image4998862.html')
    }

    imagePage_visit_360(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/360-degree-panoramic-view-of-360-view-of-red-car-river-park-in-los-angeles-california-image225478165.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/360-degree-panoramic-view-of-360-view-of-red-car-river-park-in-los-angeles-california-image225478165.html')
    }

    imagePage_visit_Restriction_Logic_1(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-on-the-start-line-at-drag-race-34375088.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-on-the-start-line-at-drag-race-34375088.html')
    }

    imagePage_visit_Restriction_Logic_2(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-turin-italy-13th-dec-2014-acrobazie-del-circo-flic-credit-realy-easy-76564207.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-turin-italy-13th-dec-2014-acrobazie-del-circo-flic-credit-realy-easy-76564207.html')
    }

    imagePage_visit_Restriction_Logic_3(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-children-a-young-girl-with-an-empty-bottle-of-pills-which-she-had-20432471.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-children-a-young-girl-with-an-empty-bottle-of-pills-which-she-had-20432471.html')
    }

    imagePage_visit_Restriction_Logic_4(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/early-zodiac-ceiling-visitor-centre-jerash-jerash-governorate-jordan-middle-easts-image332063876.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/early-zodiac-ceiling-visitor-centre-jerash-jerash-governorate-jordan-middle-easts-image332063876.html')
    }

    imagePage_visit_Restriction_Logic_5(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-bloomingdales-at-christmas-dusk-545139.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-bloomingdales-at-christmas-dusk-545139.html')
    }

    imagePage_visit_Restriction_Logic_6(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-populus-tremula-aspen-trees-changing-colours-in-autumn-in-the-scottish-89016859.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-populus-tremula-aspen-trees-changing-colours-in-autumn-in-the-scottish-89016859.html')
    }

    imagePage_visit_Restriction_Logic_7(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-zora-robot-124974102.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-zora-robot-124974102.html')
    }

    imagePage_visit_Restriction_Logic_8(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-cowboys-walk-the-streets-of-tombstone-arizona-usa-1388004.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-cowboys-walk-the-streets-of-tombstone-arizona-usa-1388004.html')
    }

    imagePage_visit_Restriction_Logic_9(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-coit-tower-san-francisco-15650779.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-coit-tower-san-francisco-15650779.html')
    }

    imagePage_visit_Restriction_Logic_10(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/netherlands-amerongen-weir-and-lock-complex-in-nederrijn-river-cargo-image62644474.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/netherlands-amerongen-weir-and-lock-complex-in-nederrijn-river-cargo-image62644474.html')
    }

    imagePage_visit_Restriction_Logic_11(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-dancing-in-the-streets-of-notting-hill-gate-london-at-carnival-time-412953.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-dancing-in-the-streets-of-notting-hill-gate-london-at-carnival-time-412953.html')
    }

    imagePage_visit_Restriction_Logic_12(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/great-barrier-reef-queensland-australia-image1173657.html')
        }
        else
        cy.visit('https://intwebbrown.alamy.'+domain+'/great-barrier-reef-queensland-australia-image1173657.html')
    }

    imagePage_visit_Restriction_Logic_13(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-cheetah-acinonyx-jubatus-native-to-africa-captive-germany-72503734.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-cheetah-acinonyx-jubatus-native-to-africa-captive-germany-72503734.html')
    }

    imagePage_visit_Restriction_Logic_14(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-cheetah-acinonyx-jubatus-native-to-africa-captive-germany-72503733.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-cheetah-acinonyx-jubatus-native-to-africa-captive-germany-72503733.html')
    }

    imagePage_visit_Restriction_Logic_15(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-cheetah-acinonyx-jubatus-cub-native-to-africa-captive-germany-72503732.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-cheetah-acinonyx-jubatus-cub-native-to-africa-captive-germany-72503732.html')
    }

    imagePage_visit_Restriction_Logic_16(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/stock-photo-blue-sea-70025943.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/stock-photo-blue-sea-70025943.html')
    }

    imagePage_visit_Restriction_Logic_17(domain)
    {
        if(domain=='fr')
        {
            cy.visit('https://intwebbrown.alamyimages.'+domain+'/temples-of-different-architecture-patan-kathmandu-nepal-test6-image4998862.html')
        }
        else
            cy.visit('https://intwebbrown.alamy.'+domain+'/temples-of-different-architecture-patan-kathmandu-nepal-test6-image4998862.html')
    }

    imagePage_license_personalUse_RM(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const personalUseId=cy.get(loc.com.personalUseId)
                 const personalTitle=msgs[domain].personalTitle
                 personalUseId.click({force: true}).contains(personalTitle)

                 const personalUseTittleXpath=cy.xpath(loc.com.personalUseTittleXpath)
                 const personalDesc=msgs[domain].personalDesc
                 personalUseTittleXpath.contains(personalDesc)
                 

                 const licenseToolTipXpath=cy.xpath(loc.com.licenseToolTipXpath)
                 licenseToolTipXpath.trigger('mouseover')

                 const licenseToolTipWrapperId=cy.get(loc.com.licenseToolTipWrapperId)
                 licenseToolTipWrapperId.contains(personalDesc)
                
              })
            })
        return this

    }

   imagePage_license_personalUse_price_check_RM(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const personalUsePriceXpath=cy.xpath(loc.com.personalUsePriceXpath)
                 const personalUsePrice=msgs[domain].personalUsePrice                
                 personalUsePriceXpath.contains(personalUsePrice)

              })
            })
        return this
    }

    imagePage_license_presentation_RM(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const presentationLicenseId=cy.get(loc.com.presentationLicenseId)
                 const presentationLicenseTitle=msgs[domain].presentationLicenseTitle
                 presentationLicenseId.click({force: true}).contains(presentationLicenseTitle)

                 const presentationLicenseTitleXpath=cy.xpath(loc.com.presentationLicenseTitleXpath)
                 const presentationLicenseDesc=msgs[domain].presentationLicenseDesc
                 presentationLicenseTitleXpath.contains(presentationLicenseDesc)

                 const presentationLicenseToolTipXpath=cy.xpath(loc.com.presentationLicenseToolTipXpath)
                 presentationLicenseToolTipXpath.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(presentationLicenseDesc)
              })
            })
        return this

    }

   imagePage_license_presentation_price_check_RM(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const presentationLicensePriceXpath=cy.xpath(loc.com.presentationLicensePriceXpath)
                 const presentationUsePrice=msgs[domain].presentationUsePrice                
                 presentationLicensePriceXpath.contains(presentationUsePrice)

              })
            })
        return this
    }

    imagePage_license_website_RM(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const websiteLicenseId=cy.get(loc.com.websiteLicenseId)
                 const websiteLicenseTitle=msgs[domain].websiteLicenseTitle
                 websiteLicenseId.click({force: true}).contains(websiteLicenseTitle)

                 const websiteLicenseDescXpath=cy.xpath(loc.com.websiteLicenseDescXpath)
                 const websiteLicenseDesc=msgs[domain].websiteLicenseDesc
                 websiteLicenseDescXpath.contains(websiteLicenseDesc)

                 const websiteLicenseToolTipXpath=cy.xpath(loc.com.websiteLicenseToolTipXpath)
                 websiteLicenseToolTipXpath.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(websiteLicenseDesc)
              })
            })
        return this
    }

    imagePage_license_website_price_check_RM(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const websiteLicensePriceXpath=cy.xpath(loc.com.websiteLicensePriceXpath)
                 const websiteUsePrice=msgs[domain].websiteUsePrice                
                 websiteLicensePriceXpath.contains(websiteUsePrice)

              })
            })
        return this
    }

    imagePage_license_Magazines_and_books_RM(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const magazineLicenseId=cy.get(loc.com.magazineLicenseId)
                 const magazineLicenseTitle=msgs[domain].magazineLicenseTitle
                 magazineLicenseId.click({force: true}).contains(magazineLicenseTitle)

                 const magazineLicenseDescXpath=cy.xpath(loc.com.magazineLicenseDescXpath)
                 const magazineLicenseDesc=msgs[domain].magazineLicenseDesc
                 magazineLicenseDescXpath.contains(magazineLicenseDesc)

                 const magazineLicenseToolTipXpath=cy.xpath(loc.com.magazineLicenseToolTipXpath)
                 magazineLicenseToolTipXpath.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(magazineLicenseDesc)
              })
            })
        return this
    }

    imagePage_license_Magazines_and_books_price_check_RM(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const magazineLicensePriceXpath=cy.xpath(loc.com.magazineLicensePriceXpath)
                 const magazineUsePrice=msgs[domain].magazineUsePrice                
                 magazineLicensePriceXpath.contains(magazineUsePrice)

              })
            })
        return this
    }

    imagePage_license_Marketing_package_Small_business_RM(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const marketingSmallBusinessLicenseId=cy.get(loc.com.marketingSmallBusinessLicenseId)
                 const marketingSmallBusinessLicenseTitle=msgs[domain].marketingSmallBusinessLicenseTitle
                 marketingSmallBusinessLicenseId.click({force: true}).contains(marketingSmallBusinessLicenseTitle)

                 const marketingSmallBusinessLicenseDescXpath=cy.xpath(loc.com.marketingSmallBusinessLicenseDescXpath)
                 const marketingSmallBusinessLicenseDesc=msgs[domain].marketingSmallBusinessLicenseDesc
                 marketingSmallBusinessLicenseDescXpath.contains(marketingSmallBusinessLicenseDesc)

                 const marketingSmallBusinessLicenseToolTipXpath=cy.xpath(loc.com.marketingSmallBusinessLicenseToolTipXpath)
                 marketingSmallBusinessLicenseToolTipXpath.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(marketingSmallBusinessLicenseDesc)
              })
            })
        return this
    }

    imagePage_license_Marketing_package_Small_business_price_check_RM(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const marketingSmallBusinessLicensePriceXpath=cy.xpath(loc.com.marketingSmallBusinessLicensePriceXpath)
                 const marketingSmallBusinessUsePrice=msgs[domain].marketingSmallBusinessUsePrice                
                 marketingSmallBusinessLicensePriceXpath.contains(marketingSmallBusinessUsePrice)

              })
            })
        return this
    }

    imagePage_license_Marketing_package_Large_business_RM(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const marketingLargeBusinessLicenseId=cy.get(loc.com.marketingLargeBusinessLicenseId)
                 const marketingLargeBusinessLicenseTitle=msgs[domain].marketingLargeBusinessLicenseTitle
                 marketingLargeBusinessLicenseId.click({force: true}).contains(marketingLargeBusinessLicenseTitle)

                 const marketingLargeBusinessLicenseDescXpath=cy.xpath(loc.com.marketingLargeBusinessLicenseDescXpath)
                 const marketingLargeBusinessLicenseDesc=msgs[domain].marketingLargeBusinessLicenseDesc
                 marketingLargeBusinessLicenseDescXpath.contains(marketingLargeBusinessLicenseDesc)

                 const marketingLargeBusinessLicenseToolTipXpath=cy.xpath(loc.com.marketingLargeBusinessLicenseToolTipXpath)
                 marketingLargeBusinessLicenseToolTipXpath.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(marketingLargeBusinessLicenseDesc)
              })
            })
        return this

    }

    imagePage_license_Marketing_package_Large_business_price_check_RM(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const marketingLargeBusinessLicensePriceXpath=cy.xpath(loc.com.marketingLargeBusinessLicensePriceXpath)
                 const marketingLargeBusinessUsePrice=msgs[domain].marketingLargeBusinessUsePrice                
                 marketingLargeBusinessLicensePriceXpath.contains(marketingLargeBusinessUsePrice)

              })
            })
        return this

    }

    imagePage_license_vector(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const vectorLicenseId=cy.get(loc.com.vectorLicenseId)
                 const vectorLicenseTitle=msgs[domain].vectorLicenseTitle
                 vectorLicenseId.click({force: true}).contains(vectorLicenseTitle)

                 const vectorLicenseDescXpath=cy.xpath(loc.com.vectorLicenseDescXpath)
                 const vectorLicenseDesc=msgs[domain].vectorLicenseDesc
                 vectorLicenseDescXpath.contains(vectorLicenseDesc)

                 const vectorLicenseToolTipXpath=cy.xpath(loc.com.vectorLicenseToolTipXpath)
                 vectorLicenseToolTipXpath.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(vectorLicenseDesc)
              })
            })
        return this
    }

    imagePage_license_vector_price_check(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const vectorLicensePriceXpath=cy.xpath(loc.com.vectorLicensePriceXpath)
                 const vectorPrice=msgs[domain].vectorPrice                
                 vectorLicensePriceXpath.contains(vectorPrice)

              })
            })
        return this
    }

    imagePage_license_personalUse_RF(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const personalUseIdLicenseIdRF=cy.get(loc.com.personalUseIdLicenseIdRF)
                 const personalTitle=msgs[domain].personalTitle
                 personalUseIdLicenseIdRF.click({force: true}).contains(personalTitle)

                 const personalUseTittleXpathRF=cy.xpath(loc.com.personalUseTittleXpathRF)
                 const personalDesc=msgs[domain].personalDesc
                 personalUseTittleXpathRF.contains(personalDesc)

                 const personalUseToolTipXpathRF=cy.xpath(loc.com.personalUseToolTipXpathRF)
                 personalUseToolTipXpathRF.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(personalDesc)
              })
            })
        return this
    }

    imagePage_license_personalUse_price_check_RF(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const personalUseLicensePriceXpathRF=cy.xpath(loc.com.personalUseLicensePriceXpathRF)
                 const personalUsePrice=msgs[domain].personalUsePrice                
                 personalUseLicensePriceXpathRF.contains(personalUsePrice)

              })
            })
        return this
    }

    imagePage_license_presentation_RF(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const presentationLicenseIdRF=cy.get(loc.com.presentationLicenseIdRF)
                 const presentationLicenseTitle=msgs[domain].presentationLicenseTitle
                 presentationLicenseIdRF.click({force: true}).contains(presentationLicenseTitle)

                 const presentationTittleXpathRF=cy.xpath(loc.com.presentationTittleXpathRF)
                 const presentationLicenseDesc=msgs[domain].presentationLicenseDesc
                 presentationTittleXpathRF.contains(presentationLicenseDesc)

                 const presentationToolTipXpathRF=cy.xpath(loc.com.presentationToolTipXpathRF)
                 presentationToolTipXpathRF.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(presentationLicenseDesc)
              })
            })
        return this 
    }

    imagePage_license_presentation_price_check_RF(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const presentationLicensePriceXpathRF=cy.xpath(loc.com.presentationLicensePriceXpathRF)
                 const presentationUsePrice=msgs[domain].presentationUsePrice                
                 presentationLicensePriceXpathRF.contains(presentationUsePrice)

              })
            })
        return this
    }

    imagePage_license_website_RF(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const websiteLicenseIdRF=cy.get(loc.com.websiteLicenseIdRF)
                 const websiteLicenseTitle=msgs[domain].websiteLicenseTitle
                 websiteLicenseIdRF.click({force: true}).contains(websiteLicenseTitle)

                 const websiteTittleXpathRF=cy.xpath(loc.com.websiteTittleXpathRF)
                 const websiteLicenseDesc=msgs[domain].websiteLicenseDesc
                 websiteTittleXpathRF.contains(websiteLicenseDesc)

                 const websiteToolTipXpathRF=cy.xpath(loc.com.websiteToolTipXpathRF)
                 websiteToolTipXpathRF.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(websiteLicenseDesc)
              })
            })
        return this 
    }

    imagePage_license_website_price_check_RF(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const websiteLicensePriceXpathRF=cy.xpath(loc.com.websiteLicensePriceXpathRF)
                 const websiteUsePrice=msgs[domain].websiteUsePrice                
                 websiteLicensePriceXpathRF.contains(websiteUsePrice)

              })
            })
        return this
    }

    imagePage_license_Magazines_and_books_RF(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const magazineLicenseIdRF=cy.get(loc.com.magazineLicenseIdRF)
                 const magazineLicenseTitle=msgs[domain].magazineLicenseTitle
                 magazineLicenseIdRF.click({force: true}).contains(magazineLicenseTitle)

                 const magazineTittleXpathRF=cy.xpath(loc.com.magazineTittleXpathRF)
                 const magazineLicenseDesc=msgs[domain].magazineLicenseDesc
                 magazineTittleXpathRF.contains(magazineLicenseDesc)

                 const magazineToolTipXpathRF=cy.xpath(loc.com.magazineToolTipXpathRF)
                 magazineToolTipXpathRF.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(magazineLicenseDesc)
              })
            })
        return this 
    }

    imagePage_license_Magazines_and_books_price_check_RF(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const magazineLicensePriceXpathRF=cy.xpath(loc.com.magazineLicensePriceXpathRF)
                 const magazineUsePrice=msgs[domain].magazineUsePrice                
                 magazineLicensePriceXpathRF.contains(magazineUsePrice)

              })
            })
        return this

    }

    imagePage_license_Marketing_package_Small_business_RF(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const marketingSmallBusinessLicenseIdRF=cy.get(loc.com.marketingSmallBusinessLicenseIdRF)
                 const marketingSmallBusinessLicenseTitle=msgs[domain].marketingSmallBusinessLicenseTitle
                 marketingSmallBusinessLicenseIdRF.click({force: true}).contains(marketingSmallBusinessLicenseTitle)

                 const marketingSmallBusinessTittleXpathRF=cy.xpath(loc.com.marketingSmallBusinessTittleXpathRF)
                 const marketingSmallBusinessLicenseDesc=msgs[domain].marketingSmallBusinessLicenseDesc
                 marketingSmallBusinessTittleXpathRF.contains(marketingSmallBusinessLicenseDesc)

                 const marketingSmallBusinessToolTipXpathRF=cy.xpath(loc.com.marketingSmallBusinessToolTipXpathRF)
                 marketingSmallBusinessToolTipXpathRF.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(marketingSmallBusinessLicenseDesc)
              })
            })
        return this 

    }

    imagePage_license_Marketing_package_Small_business_price_check_RF(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const marketingSmallBusinessLicensePriceXpathRF=cy.xpath(loc.com.marketingSmallBusinessLicensePriceXpathRF)
                 const marketingSmallBusinessUsePrice=msgs[domain].marketingSmallBusinessUsePrice                
                 marketingSmallBusinessLicensePriceXpathRF.contains(marketingSmallBusinessUsePrice)

              })
            })
        return this

    }

    imagePage_license_Marketing_package_Large_business_RF(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const marketingLargeBusinessLicenseIdRF=cy.get(loc.com.marketingLargeBusinessLicenseIdRF)
                 const marketingLargeBusinessLicenseTitle=msgs[domain].marketingLargeBusinessLicenseTitle
                 marketingLargeBusinessLicenseIdRF.click({force: true}).contains(marketingLargeBusinessLicenseTitle)

                 const marketingLargeBusinessTittleXpathRF=cy.xpath(loc.com.marketingLargeBusinessTittleXpathRF)
                 const marketingLargeBusinessLicenseDesc=msgs[domain].marketingLargeBusinessLicenseDesc
                 marketingLargeBusinessTittleXpathRF.contains(marketingLargeBusinessLicenseDesc)

                 const marketingLargeBusinessToolTipXpathRF=cy.xpath(loc.com.marketingLargeBusinessToolTipXpathRF)
                 marketingLargeBusinessToolTipXpathRF.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(marketingLargeBusinessLicenseDesc)
              })
            })
        return this 
    }

    imagePage_license_Marketing_package_Large_business_price_check_RF(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const marketingLargeBusinessLicensePriceXpathRF=cy.xpath(loc.com.marketingLargeBusinessLicensePriceXpathRF)
                 const marketingLargeBusinessUsePrice=msgs[domain].marketingLargeBusinessUsePrice                
                 marketingLargeBusinessLicensePriceXpathRF.contains(marketingLargeBusinessUsePrice)

              })
            })
        return this      
    }

    imagePage_license_Website_or_social_media_site_360(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const websiteOrSocialMediaLicenseId360=cy.get(loc.com.websiteOrSocialMediaLicenseId360)
                 const websiteOrSocialMediaLicenseTitle=msgs[domain].websiteOrSocialMediaLicenseTitle
                 websiteOrSocialMediaLicenseId360.click({force: true}).contains(websiteOrSocialMediaLicenseTitle)

                 const websiteOrSocialMediaTittleXpath360=cy.xpath(loc.com.websiteOrSocialMediaTittleXpath360)
                 const websiteOrSocialMediaLicenseDesc=msgs[domain].websiteOrSocialMediaLicenseDesc
                 websiteOrSocialMediaTittleXpath360.contains(websiteOrSocialMediaLicenseDesc)

                 const websiteOrSocialMediaToolTipXpath360=cy.xpath(loc.com.websiteOrSocialMediaToolTipXpath360)
                 websiteOrSocialMediaToolTipXpath360.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(websiteOrSocialMediaLicenseDesc)
              })
            })
        return this   
    }

    imagePage_license_Website_or_social_media_site_price_check_360(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const websiteOrSocialMediaLicensePriceXpath360=cy.xpath(loc.com.websiteOrSocialMediaLicensePriceXpath360)
                 const websiteOrSocialMediaPrice=msgs[domain].websiteOrSocialMediaPrice                
                 websiteOrSocialMediaLicensePriceXpath360.contains(websiteOrSocialMediaPrice)

              })
            })
        return this
    }

    imagePage_license_VR_or_game_apps_360(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const VRorGameAppsLicenseId360=cy.get(loc.com.VRorGameAppsLicenseId360)
                 const VRorGameAppsLicenseTitle=msgs[domain].VRorGameAppsLicenseTitle
                 VRorGameAppsLicenseId360.click({force: true}).contains(VRorGameAppsLicenseTitle)

                 const VRorGameAppsLicenseTitleXpath360=cy.xpath(loc.com.VRorGameAppsLicenseTitleXpath360)
                 const VRorGameAppsLicenseDesc=msgs[domain].VRorGameAppsLicenseDesc
                 VRorGameAppsLicenseTitleXpath360.contains(VRorGameAppsLicenseDesc)

                 const VRorGameAppsToolTipXpath360=cy.xpath(loc.com.VRorGameAppsToolTipXpath360)
                 VRorGameAppsToolTipXpath360.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(VRorGameAppsLicenseDesc)
              })
            })
        return this 
    }

    imagePage_license_VR_or_game_apps_price_check_360(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const VRorGameAppsLicensePriceXpath360=cy.xpath(loc.com.VRorGameAppsLicensePriceXpath360)
                 const VRorGameAppsPrice=msgs[domain].VRorGameAppsPrice                
                 VRorGameAppsLicensePriceXpath360.contains(VRorGameAppsPrice)

              })
            })
        return this
    }

    imagePage_license_Digital_publishing_360(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const digitalPublishingLicenseId360=cy.get(loc.com.digitalPublishingLicenseId360)
                 const digitalPublishingLicenseTitle=msgs[domain].digitalPublishingLicenseTitle
                 digitalPublishingLicenseId360.click({force: true}).contains(digitalPublishingLicenseTitle)

                 const digitalPublishingTitleXpath360=cy.xpath(loc.com.digitalPublishingTitleXpath360)
                 const digitalPublishingLicenseDesc=msgs[domain].digitalPublishingLicenseDesc
                 digitalPublishingTitleXpath360.contains(digitalPublishingLicenseDesc)

                 const digitalPublishingToolTipXpath360=cy.xpath(loc.com.digitalPublishingToolTipXpath360)
                 digitalPublishingToolTipXpath360.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(digitalPublishingLicenseDesc)
              })
            })
        return this 
    }

    imagePage_license_Digital_publishing_price_check_360(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const digitalPublishingPriceXpath360=cy.xpath(loc.com.digitalPublishingPriceXpath360)
                 const digitalPublishingPrice=msgs[domain].digitalPublishingPrice                
                 digitalPublishingPriceXpath360.contains(digitalPublishingPrice)

              })
            })
        return this
    }

    imagePage_license_TV_program_360(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const TVProgramLicenseId360=cy.get(loc.com.TVProgramLicenseId360)
                 const TVProgramLicenseTitle=msgs[domain].TVProgramLicenseTitle
                 TVProgramLicenseId360.click({force: true}).contains(TVProgramLicenseTitle)

                 const TVProgramTitleXpath360=cy.xpath(loc.com.TVProgramTitleXpath360)
                 const TVProgramLicenseDesc=msgs[domain].TVProgramLicenseDesc
                 TVProgramTitleXpath360.contains(TVProgramLicenseDesc)

                 const TVProgramToolTipXpath360=cy.xpath(loc.com.TVProgramToolTipXpath360)
                 TVProgramToolTipXpath360.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(TVProgramLicenseDesc)
              })
            })
        return this
    }

    imagePage_license_TV_program_price_check_360(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const TVProgramPriceXpath360=cy.xpath(loc.com.TVProgramPriceXpath360)
                 const TVProgramPrice=msgs[domain].TVProgramPrice                
                 TVProgramPriceXpath360.contains(TVProgramPrice)

              })
            })
        return this
    }

    imagePage_license_Marketing_package_Small_business_360(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const MarketingPackageSmallBusinessLicenseId360=cy.get(loc.com.MarketingPackageSmallBusinessLicenseId360)
                 const MarketingPackageSmallBusinessLicenseTitle=msgs[domain].MarketingPackageSmallBusinessLicenseTitle
                 MarketingPackageSmallBusinessLicenseId360.click({force: true}).contains(MarketingPackageSmallBusinessLicenseTitle)

                 const MarketingPackageSmallBusinessTitleXpath360=cy.xpath(loc.com.MarketingPackageSmallBusinessTitleXpath360)
                 const MarketingPackageSmallBusinessLicenseDesc=msgs[domain].MarketingPackageSmallBusinessLicenseDesc
                 MarketingPackageSmallBusinessTitleXpath360.contains(MarketingPackageSmallBusinessLicenseDesc)

                 const MarketingPackageSmallBusinessToolTipXpath360=cy.xpath(loc.com.MarketingPackageSmallBusinessToolTipXpath360)
                 MarketingPackageSmallBusinessToolTipXpath360.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(MarketingPackageSmallBusinessLicenseDesc)
              })
            })
        return this
    }

    imagePage_license_Marketing_package_Small_business_price_check_360(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const MarketingPackageSmallBusinessPriceXpath360=cy.xpath(loc.com.MarketingPackageSmallBusinessPriceXpath360)
                 const MarketingPackageSmallBusinessPrice=msgs[domain].MarketingPackageSmallBusinessPrice                
                 MarketingPackageSmallBusinessPriceXpath360.contains(MarketingPackageSmallBusinessPrice)

              })
            })
        return this

    }

    imagePage_license_Marketing_package_Large_business_360(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const MarketingPackageLargeBusinessLicenseId360=cy.get(loc.com.MarketingPackageLargeBusinessLicenseId360)
                 const MarketingPackageLargeBusinessLicenseTitle=msgs[domain].MarketingPackageLargeBusinessLicenseTitle
                 MarketingPackageLargeBusinessLicenseId360.click({force: true}).contains(MarketingPackageLargeBusinessLicenseTitle)

                 const MarketingPackageLargeBusinessTitleXpath360=cy.xpath(loc.com.MarketingPackageLargeBusinessTitleXpath360)
                 const MarketingPackageLargeBusinessLicenseDesc=msgs[domain].MarketingPackageLargeBusinessLicenseDesc
                 MarketingPackageLargeBusinessTitleXpath360.contains(MarketingPackageLargeBusinessLicenseDesc)

                 const MarketingPackageLargeBusinessToolTipXpath360=cy.xpath(loc.com.MarketingPackageLargeBusinessToolTipXpath360)
                 MarketingPackageLargeBusinessToolTipXpath360.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(MarketingPackageLargeBusinessLicenseDesc)
              })
            })
        return this
    }

    imagePage_license_Marketing_package_Large_business_price_check_360(domain)
    {
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {

                 const MarketingPackageLargeBusinessPriceXpath360=cy.xpath(loc.com.MarketingPackageLargeBusinessPriceXpath360)
                 const MarketingPackageLargeBusinessPrice=msgs[domain].MarketingPackageLargeBusinessPrice                
                 MarketingPackageLargeBusinessPriceXpath360.contains(MarketingPackageLargeBusinessPrice)

              })
            })
        return this

    }

    imagePage_license_Editorial_Website(domain)
    {
        cy.wait(500)
        cy.fixture('locator').as('alamyLocator');
        cy.fixture('validationmsgs').as('validmsgs');
        cy.get('@validmsgs').then(msgs=>
            {
            cy.get('@alamyLocator').then(loc=>
              {
                 const EditorialWebsiteLicenseId=cy.get(loc.com.EditorialWebsiteLicenseId)
                 const EditorialWebsiteTitle=msgs[domain].EditorialWebsiteTitle
                 EditorialWebsiteLicenseId.click({force: true}).contains(EditorialWebsiteTitle)

                 const EditorialWebsiteTittleXpath=cy.xpath(loc.com.EditorialWebsiteTittleXpath)
                 const EditorialWebsiteDesc=msgs[domain].EditorialWebsiteDesc
                 EditorialWebsiteTittleXpath.contains(EditorialWebsiteDesc)

                 const EditorialWebsiteToolTipXpath360=cy.xpath(loc.com.EditorialWebsiteToolTipXpath360)
                 EditorialWebsiteToolTipXpath360.trigger('mouseover')

                 const presentationLicenseToolTipWrapperId=cy.get(loc.com.presentationLicenseToolTipWrapperId)
                 presentationLicenseToolTipWrapperId.contains(EditorialWebsiteDesc)
              })
            })
        return this 
    
    }

    imagePage_personalUse_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.personalUseId).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.personalUseId).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.personalUseId).should('not.exist');
            })
        }
 
    }

    imagePage_presentation_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.presentationLicenseId).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.presentationLicenseId).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.presentationLicenseId).should('not.exist');
            })
        }
    }

    imagePage_website_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.websiteLicenseId).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.websiteLicenseId).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.websiteLicenseId).should('not.exist');
            })
        }
    }

    imagePage_magazine_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.magazineLicenseId).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.magazineLicenseId).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.magazineLicenseId).should('not.exist');
            })
        }
 
    }

    imagePage_marketingSmallBusiness_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.marketingSmallBusinessLicenseId).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.marketingSmallBusinessLicenseId).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.marketingSmallBusinessLicenseId).should('not.exist');
            })
        }
 
    }

    imagePage_marketingLargeBusiness_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.marketingLargeBusinessLicenseId).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.marketingLargeBusinessLicenseId).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.marketingLargeBusinessLicenseId).should('not.exist');
            })
        }
 
    }

    imagePage_license_Editorial_Website_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.EditorialWebsiteLicenseId).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.EditorialWebsiteLicenseId).invoke('attr', 'class').should('contains', 'disable')
            })
        }

        else
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.EditorialWebsiteLicenseId).should('not.exist');
            })
        }
 
    }

    imagePage_license_Editorial_presentation_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.EditorialPresentationLicenseId).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.EditorialPresentationLicenseId).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.EditorialPresentationLicenseId).should('not.exist');
            })
        }
 
    }

    imagePage_license_Editorial_magazine_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.EditorialMagazineLicenseId).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.EditorialMagazineLicenseId).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.EditorialMagazineLicenseId).should('not.exist');
            })
        }
    }

    imagePage_license_PersonalUse_Editorial_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.personalUseEditorialLicenseId).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.personalUseEditorialLicenseId).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else 
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.personalUseEditorialLicenseId).should('not.exist');
            })
        }
 
    }

    imagePage_license_marketingSmallBusiness_Editorial_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.marketingPackageSmallBusinessEditorialLicenseId).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.marketingPackageSmallBusinessEditorialLicenseId).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else 
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.marketingPackageSmallBusinessEditorialLicenseId).should('not.exist');
            })
        }
 
    }

    imagePage_license_marketingLargeBusiness_Editorial_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.marketingPackageLargeBusinessEditorialLicenseId).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.marketingPackageLargeBusinessEditorialLicenseId).invoke('attr', 'class').should('contains', 'disable')
            })
        }

        else 
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.marketingPackageLargeBusinessEditorialLicenseId).should('not.exist');
            })
        }
 
    }

    imagePage_license_personalUse_Editorial_RF_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.personalUseIdLicenseIdRF).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.personalUseIdLicenseIdRF).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else 
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.personalUseIdLicenseIdRF).should('not.exist');
            })
        }
 
    }

    imagePage_license_personalUse_Editorial_RF_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.personalUseIdLicenseIdRF).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.personalUseIdLicenseIdRF).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else 
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.personalUseIdLicenseIdRF).should('not.exist');
            })
        }
 
    }

    imagePage_license_presentation_Editorial_RF_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.presentationLicenseIdRF).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.presentationLicenseIdRF).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else 
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.presentationLicenseIdRF).should('not.exist');
            })
        }
 
    }

    imagePage_license_website_Editorial_RF_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.websiteLicenseIdRF).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.websiteLicenseIdRF).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else 
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.websiteLicenseIdRF).should('not.exist');
            })
        }
 
    }

    imagePage_license_magazine_Editorial_RF_Restriction(isDisabled)
    {
        if(isDisabled==0)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.magazineLicenseIdRF).invoke('attr', 'class').should('not.contains', 'disable')
            })
        }
        else if(isDisabled==1)
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
            cy.get(loc.com.magazineLicenseIdRF).invoke('attr', 'class').should('contains', 'disable')
            })
        }
        else 
        {
            cy.fixture('locator').as('alamyLocator');
            cy.get('@alamyLocator').then(loc=>
            {
                cy.get(loc.com.magazineLicenseIdRF).should('not.exist');
            })
        }
 
    }

}
export default ImagePage